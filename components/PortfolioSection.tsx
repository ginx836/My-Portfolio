import React from "react";
import { portfolios } from "@/constants";



const PortfolioSection = ({
  data,
  title,
}: {
  data: typeof portfolios;
  title: string;
}) => {
  return <div>PFSection</div>;
};

export default PortfolioSection;
